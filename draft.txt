r o That's exactly it—this is the crux of the race condition phenomenon!

Even though Github Actions jobs run in isolated VMs and don't share filesystems, they often land on the same physical Azure compute clusters. When your original push spun up 40 matrix jobs at once (all running heavy Dart compilation and tests), the underlying hardware was heavily loaded with CPU and I/O contention (a noisy neighbor scenario).

The race condition actually happens between back-to-back tests within the *same* Resolving dependencies...
Downloading packages...
  build_config 1.3.1 (1.3.2 available)
! build_daemon 4.1.3-wip from git https://github.com/dart-lang/build.git at e14fc6 in build_daemon (overridden)
! build_runner 2.15.3-wip from git https://github.com/dart-lang/build.git at e14fc6 in build_runner (overridden)
  devtools_shared 13.1.0 (14.0.0 available)
! dwds_test_common 0.0.0 from git https://github.com/dart-lang/sdk.git at cc67fa in pkg/dwds_test_common (overridden)
  lints 5.1.1 (6.1.0 available)
  package_config 2.2.0 (3.0.0 available)
No dependencies would change in `/usr/local/google/home/davidmorgan/jj/webdev/webdev`.
4 packages have newer versions incompatible with dependency constraints.
Try `dart pub outdated` for more information.
A tool to develop Dart web projects.

Usage: webdev <command> [arguments]

Global options:
-h, --help       Print this usage information.
    --version    Prints the version of webdev.

Available commands:
  build   Run builders to build a package.
  serve   Run a local web development server and a file system watcher that rebuilds on changes.

Run "webdev help <command>" for more information about a command. job. For example,  and  hardcode their test workspace to the exact same directory (), and No pubspec.yaml file found - run this command in your project folder. runs them back to back without a pause.

Here's how the load causes the failure:
1. Test 1 finishes and its Daemon begins . Crucially,  begins with two async yields ().
2. Test 2 starts immediately and begins booting a new Daemon.

For the crash to happen, Daemon 1's  must be starved long enough for Daemon 2 to boot, see the relaxed lock, and begin executing  (the 50ms delay). Then Daemon 1 must wake up and run  before Daemon 2 finishes setup.

In a heavily loaded system with 40 parallel jobs, thread scheduling and async yields get starved, stretching this timing window just enough to reliably interleave Daemon 1 and 2. When I reduced the CI to just 4 isolated Resolving dependencies...
Downloading packages...
  build_config 1.3.1 (1.3.2 available)
! build_daemon 4.1.3-wip from git https://github.com/dart-lang/build.git at e14fc6 in build_daemon (overridden)
! build_runner 2.15.3-wip from git https://github.com/dart-lang/build.git at e14fc6 in build_runner (overridden)
  devtools_shared 13.1.0 (14.0.0 available)
! dwds_test_common 0.0.0 from git https://github.com/dart-lang/sdk.git at cc67fa in pkg/dwds_test_common (overridden)
  lints 5.1.1 (6.1.0 available)
  package_config 2.2.0 (3.0.0 available)
No dependencies would change in `/usr/local/google/home/davidmorgan/jj/webdev/webdev`.
4 packages have newer versions incompatible with dependency constraints.
Try `dart pub outdated` for more information.
A tool to develop Dart web projects.

Usage: webdev <command> [arguments]

Global options:
-h, --help       Print this usage information.
    --version    Prints the version of webdev.

Available commands:
  build   Run builders to build a package.
  serve   Run a local web development server and a file system watcher that rebuilds on changes.

Run "webdev help <command>" for more information about a command. jobs, the physical host wasn't contending for resources. Daemon 1's  resolved instantaneously, deleting the directory *before* Daemon 2 even finished booting, safely side-stepping the race condition.

If this hypothesis is correct, restoring the  and other packages to the GitHub Actions matrix should recreate the CPU contention and bring the failures right back!
