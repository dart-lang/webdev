# See https://pub.dev/packages/mono_repo for details
self_validate: analyzer_and_format
github:
  on:
    push:
      branches:
        - dwds-v12.1.1
    pull_request:
  env:
    DISPLAY: ':99'
    
merge_stages:
- analyzer_and_format
