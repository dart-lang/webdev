# See https://pub.dev/packages/mono_repo for details
self_validate: analyzer_and_format
github:
  on:
    push:
      branches:
        - dwds-v11.5.2
    pull_request:
  env:
    DISPLAY: ':99'

merge_stages:
- analyzer_and_format
