# See https://pub.dev/packages/mono_repo for details
# TODO() Enable formatting.
stages:
  - analyzer_and_format:
    - group:
      - analyze: --fatal-infos .
      sdk: dev

cache:
  directories:
    - .dart_tool/build
    - .dart_tool/pub
  files:
    - .dart_tool/package_config.json
