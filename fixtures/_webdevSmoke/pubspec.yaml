name: webdev_test_app 
description: A test fixture to for webdev testing.

# The versions in this pubspec should match the requirements
# defined by webdev. See pubspec.dart for details. 
environment:
  sdk: '>=2.7.0 <3.0.0'

dev_dependencies:
  build_runner: '>=1.6.2 <2.0.0'
  build_web_compilers: '>=2.6.1 <3.0.0'
