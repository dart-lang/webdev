name: _test
version: 1.0.0
description: >-
  A fake package used for testing
publish_to: none

environment:
  sdk: ">=2.5.0 <3.0.0"

dependencies:
  intl: ^0.16.0
  path: ^1.6.1

dev_dependencies:
  build_runner: ^1.6.7
  build_web_compilers: ^2.12.0
