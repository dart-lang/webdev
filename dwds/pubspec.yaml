name: dwds
version: 0.1.0-dev
description: >-
  A service that proxies between the Chrome debug protocol and the vm
  service protocol.
environment:
  sdk: ^2.1.0
dependencies:
  source_maps: ^0.10.0
  vm_service_lib: ^3.14.3-dev.1
  webkit_inspection_protocol: ^0.3.6
dev_dependencies:
  args: ^1.0.0
  build_runner: ^1.0.0
  build_web_compilers: ^1.0.0
  test: ^1.0.0
  webdev:
    path: ../webdev
